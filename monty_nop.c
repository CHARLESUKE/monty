#include "monty.h"
/**
  *f_nop- this function is nothing
  *@headboy: this variable is the stack head
  *@count: this variable is the line_number
  *Return: absolutely no return
 */
void f_nop(stack_t **headboy, unsigned int count)
{
	(void) count;
	(void) headboy;
}
