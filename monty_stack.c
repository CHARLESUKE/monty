#include "monty.h"
/**
 * f_stack - this main function prints the top
 * @headboy: this variable is the stack head
 * @count: this variable is the line_number
 * Return: absolutely no return
*/
void f_stack(stack_t **headboy, unsigned int count)
{
	(void)headboy;
	(void)count;
	bus.lifi = 0;
}
